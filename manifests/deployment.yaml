# /root/test-gitops-repo/manifests/deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-hello-app
  labels:
    app: hello-world
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hello-world
  template:
    metadata:
      labels:
        app: hello-world
    spec:
      # 프라이빗 레지스트리 인증 정보 사용
      imagePullSecrets:
      - name: harbor-registry-secret
      containers:
      - name: webserver
        # 💡 중요: Harbor 주소 및 프로젝트/이미지 이름
        image: 172.16.8.200:30003/test/test:v1.0
        ports:
        - containerPort: 8080
